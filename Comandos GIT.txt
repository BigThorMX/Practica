git clone git@github.com:BigThorMX/Practica.git
git add . (el punto indica todo el add es agregar los cambios al repositorio)
git status "indica el status de los cambios"
git commit -m "Mi primer commit" (agregar descripcion a los cambios)
git log 
git branch
git push origin main


Pasos para subir un commit 
git add . 
git commit -m "Mi primer commit"
git push origin main

"Crear una nueva branch"
git checkout -b nombre-branch 
git reset --hard "me resetea a mi ultimo commit"
git pull "me sube lo que tengo en linea a la pc"
git checkout nombre-branch "me cambia a una branch que ya existe"